/*
 * EditView Messages
 *
 * This contains all the text for the EditView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditView container!',
  },
});
