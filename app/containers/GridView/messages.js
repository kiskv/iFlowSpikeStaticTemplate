/*
 * GridView Messages
 *
 * This contains all the text for the GridView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GridView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GridView container!',
  },
});
